/**
 * Phosphor icons (the icon family used by the EST SA design). The SSR build
 * renders without React context, so the same components work on the server
 * and the client. Re-exported here so the icon set stays in one place.
 */
export {
  ArrowDown,
  ArrowRight,
  Barcode,
  BellSimple,
  Check,
  CheckCircle,
  DeviceMobile,
  IdentificationCard,
  List,
  LockSimple,
  LockSimpleOpen,
  MagnifyingGlass,
  Minus,
  Package,
  Plus,
  Printer,
  Receipt,
  ShoppingCartSimple,
  SignOut,
  SquaresFour,
  Stack,
  Storefront,
  Tag,
  Trash,
  Truck,
  WarningCircle,
  X,
} from '@phosphor-icons/react/dist/ssr';
